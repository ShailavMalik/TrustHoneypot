# ═══════════════════════════════════════════════════════════════
# Honeypot API — Environment Variables Template
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# All variables have sensible defaults for local development.
# Only API_KEY is required to be changed for production.
# ═══════════════════════════════════════════════════════════════

# API key for authenticating requests (sent via x-api-key header)
# Change this to a strong secret in production!
API_KEY=your-secret-api-key-here

# Callback URL — the endpoint where final results are sent
# Default: GUVI hackathon evaluation endpoint
CALLBACK_URL=https://hackathon.guvi.in/api/updateHoneyPotFinalResult

# Server port (used by Procfile / Railway deployment)
PORT=8000
